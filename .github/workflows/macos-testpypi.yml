name: MacOS wheel TestPyPI

on: [push]

jobs:
  build_wheel_macos:
    name: Build wheel on MacOS-10.15
    runs-on: macos-10.15
    strategy:
      fail-fast: false
    steps:
      - uses: actions/checkout@v3

      - name: Build Python wheel
        run: |
          chmod -R +x tools/* \
          && tools/macos-wheels.sh
      - uses: actions/upload-artifact@v3
        with:
          name: wheels
          path: ./wheelhouse